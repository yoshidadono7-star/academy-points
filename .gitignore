# Dependencies
node_modules/

# Firebase
.firebase/
firebase-debug.log
# Note: .firebaserc is committed (contains only the public project ID "hero-s-points")
# so that `firebase deploy` works without --project flag on every developer's machine.

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Environment / Secrets
.env
.env.local
.claude/
docs/*.pdf
